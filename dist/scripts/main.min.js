document.addEventListener("DOMContentLoaded",function(){document.getElementById("form-sorteador").addEventListener("submit",function(evento){evento.preventDefault();let numeroMaximo=parseInt(numeroMaximo);evento=math.random()*numeroMaximo,evento=math.floor(1+evento);document.getElementById("resultado-valor").innerText=evento,document.querySelector(".resultado").style.display})});